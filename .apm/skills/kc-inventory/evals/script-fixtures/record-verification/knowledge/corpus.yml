contract_version: 1
tracked_data:
  root: data
local_data:
  local_file_pattern: "*.local.*"
source_units:
  document:
    unit: document
    path_pattern: data/<source>/documents/<slug>
workflow_stages: [indexed]
